/** @type {import('next').NextConfig} */
const nextConfig = {
    //devIndicators: false
    output: 'export',
    basePath: '/diff-tool',
    images: {
        unoptimized: true
    }
};

export default nextConfig;

